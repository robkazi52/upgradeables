# Structured State Projection — Basic Example

**Evidence label:** Illustrative modern example. It demonstrates the v0.2 operational contract and is not presented as a recovered historical chat.

## Task context

A citation checker reviews a report produced from sensitive case state.

## Why this Upgradeable activates

It needs claims and source pointers, not identities or strategy notes.

## Inputs / state

Canonical version 17 with claim, source, identity, permissions, and strategy fields.

## What it does

Projects claims, citations, relevant constraints, and version metadata with no mutation rights.

## What it does not do

It does not expose identities or allow the checker to edit task authority.

## Result / state change

Citation checking occurs with minimal disclosure and safe merge boundaries.

## Interaction with companion components

['stateblock', 'domain-mode-isolation', 'scoped-loader']
