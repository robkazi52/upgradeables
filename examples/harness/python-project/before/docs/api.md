# Example API

`normalize_name(value: str, *, strict: bool = False) -> str` is public in the
1.x line. Removing it, making `strict` required, or narrowing accepted values
requires a major release and migration guidance.
