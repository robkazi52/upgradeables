# Recipe Review v0.2

All 17 recipes were reviewed against v0.2 package semantics.

| Recipe | Required | High cost | Review |
|---|---|---|:---:|
| `research-skill` | `task-set-lock-in`, `scoped-loader`, `stateblock`, `grounding-no-invention` | `multi-truth-gating`, `critical-atomic-verification` | PASS |

**Boundary:** Use when a deliverable must synthesize multiple accessible sources and corpus size or ambiguity makes scoped loading, shared state, and claim-level grounding necessary.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the research result needs stable scope, bounded retrieval, shared evidence state, and no invented gap-filling. `scoped-loader` — Keep modular OS or Skill execution relevant, ordered, and within context limits instead of loading the full library at session start. It is required here because the research result needs stable scope, bounded retrieval, shared evidence state, and no invented gap-filling. `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because the research result needs stable scope, bounded retrieval, shared evidence state, and no invented gap-filling. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because the research result needs stable scope, bounded retrieval, shared evidence state, and no invented gap-filling.

**Important exclusion:** Omit durable or parallel machinery unless corpus size or real host execution triggers it.

| `source-grounded-analysis` | `task-set-lock-in`, `mode-lock-in`, `grounding-no-invention`, `citation-fidelity` | None | PASS |

**Boundary:** Use when an analysis or rewrite must remain traceable to identified sources, preserve locked source meaning, and attach citations at claim level.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the output contract requires stable mode, source-only claims, and claim-level citation support. `mode-lock-in` — Keep behavior stable across long sessions, tool calls, and distracting inputs. It is required here because the output contract requires stable mode, source-only claims, and claim-level citation support. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because the output contract requires stable mode, source-only claims, and claim-level citation support. `citation-fidelity` — Ensure citations prove the precise nearby claim instead of functioning as decorative evidence. It is required here because the output contract requires stable mode, source-only claims, and claim-level citation support.

**Important exclusion:** Omit branching and creative expansion unless competing interpretations are an explicit deliverable.

| `high-stakes-reasoning` | `grounding-no-invention`, `epistemic-status-gating`, `risk-tier-scaling`, `critical-atomic-verification`, `multi-truth-gating`, `truth-priority-hierarchy`, `domain-mode-isolation`, `fail-closed-abstention`, `parallel-qms` | `critical-atomic-verification`, `multi-truth-gating`, `truth-redundancy`, `fail-closed-abstention`, `fermionic-veto` | PASS |

**Boundary:** Use when an error could materially affect safety, health, rights, finances, or an irreversible action and the necessary evidence can be inspected.

**Required rationale:** `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `epistemic-status-gating` — Keep mixed-certainty reasoning auditable and stop conclusions from laundering inference or hypothesis into fact. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `risk-tier-scaling` — Apply proportionate rigor so low-risk tasks remain efficient and high-risk claims or actions receive stronger evidence and fail-closed handling. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `critical-atomic-verification` — Concentrate verification on the smallest facts whose failure would invalidate the output. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `multi-truth-gating` — Reduce dependence on one fragile source, inference chain, or evaluator before a consequential conclusion is committed. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `truth-priority-hierarchy` — Resolve conflicting signals without letting fluency, optimization, or an undifferentiated vote override stronger evidence or safety authority. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `domain-mode-isolation` — Prevent cross-domain contamination while permitting explicit, reviewed transfers of shared facts. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `fail-closed-abstention` — Ensure that missing essential support produces an explicit bounded result rather than fabricated closure. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary. `parallel-qms` — Match validation topology to failure risk instead of treating QMS as one generic critic or a majority vote. It is required here because every required control protects a non-compensable evidence, domain, risk, or abstention boundary.

**Important exclusion:** Omit style and ideation modules; do not add simulated distributed evaluators.

| `medical-evidence` | `task-set-lock-in`, `grounding-no-invention`, `risk-tier-scaling`, `critical-atomic-verification`, `truth-priority-hierarchy`, `fail-closed-abstention`, `domain-mode-isolation`, `parallel-qms` | `critical-atomic-verification`, `fail-closed-abstention` | PASS |

**Boundary:** Use for source-backed medical evidence synthesis where claim scope, uncertainty, and abstention matter; it does not replace clinical diagnosis or treatment.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `risk-tier-scaling` — Apply proportionate rigor so low-risk tasks remain efficient and high-risk claims or actions receive stronger evidence and fail-closed handling. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `critical-atomic-verification` — Concentrate verification on the smallest facts whose failure would invalidate the output. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `truth-priority-hierarchy` — Resolve conflicting signals without letting fluency, optimization, or an undifferentiated vote override stronger evidence or safety authority. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `fail-closed-abstention` — Ensure that missing essential support produces an explicit bounded result rather than fabricated closure. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `domain-mode-isolation` — Prevent cross-domain contamination while permitting explicit, reviewed transfers of shared facts. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling. `parallel-qms` — Match validation topology to failure risk instead of treating QMS as one generic critic or a majority vote. It is required here because medical uncertainty and consequence require source grounding, authority ranking, domain separation, and fail-closed handling.

**Important exclusion:** Omit unconstrained generation and any validator whose evidence boundary cannot be supplied.

| `legal-evidence` | `task-set-lock-in`, `grounding-no-invention`, `risk-tier-scaling`, `critical-atomic-verification`, `truth-priority-hierarchy`, `citation-fidelity`, `fail-closed-abstention`, `parallel-qms` | `critical-atomic-verification`, `fail-closed-abstention` | PASS |

**Boundary:** Use for source-backed legal analysis tied to a declared jurisdiction and date where authority, quotation, and uncertainty must remain auditable; it is not legal advice.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `risk-tier-scaling` — Apply proportionate rigor so low-risk tasks remain efficient and high-risk claims or actions receive stronger evidence and fail-closed handling. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `critical-atomic-verification` — Concentrate verification on the smallest facts whose failure would invalidate the output. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `truth-priority-hierarchy` — Resolve conflicting signals without letting fluency, optimization, or an undifferentiated vote override stronger evidence or safety authority. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `citation-fidelity` — Ensure citations prove the precise nearby claim instead of functioning as decorative evidence. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `fail-closed-abstention` — Ensure that missing essential support produces an explicit bounded result rather than fabricated closure. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention. `parallel-qms` — Match validation topology to failure risk instead of treating QMS as one generic critic or a majority vote. It is required here because jurisdiction-sensitive claims require source grounding, authority ranking, exact citation behavior, and bounded abstention.

**Important exclusion:** Omit creative rewriting and preserve jurisdiction, date, authority, and quotation boundaries.

| `coding-debugging` | `task-set-lock-in`, `invariance-stress-scaffold`, `micro-repair` | `surgery-edit` | PASS |

**Boundary:** Use after a software defect is reproducible or tightly localized and the task requests a verified repair, not a review-only finding.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the repair must preserve task scope and invariants while starting at the smallest viable edit. `invariance-stress-scaffold` — Operationalize the recovered name without pretending the original January 2026 mechanics were recovered. It is required here because the repair must preserve task scope and invariants while starting at the smallest viable edit. `micro-repair` — Restore local correctness or completeness with the minimum semantic blast radius. It is required here because the repair must preserve task scope and invariants while starting at the smallest viable edit.

**Important exclusion:** Omit Surgery Edit while the defect remains local; omit citation controls without external sources.

| `code-review` | `task-set-lock-in`, `scoped-loader`, `grounding-no-invention`, `invariance-stress-scaffold` | `critical-atomic-verification`, `fail-closed-abstention` | PASS |

**Boundary:** Use when the requested deliverable is review-only analysis of a concrete diff or pull request for correctness, regressions, and unsupported assumptions.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because a review must stay within the supplied diff and repository evidence and must not silently become remediation. `scoped-loader` — Keep modular OS or Skill execution relevant, ordered, and within context limits instead of loading the full library at session start. It is required here because a review must stay within the supplied diff and repository evidence and must not silently become remediation. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because a review must stay within the supplied diff and repository evidence and must not silently become remediation. `invariance-stress-scaffold` — Operationalize the recovered name without pretending the original January 2026 mechanics were recovered. It is required here because a review must stay within the supplied diff and repository evidence and must not silently become remediation.

**Important exclusion:** Omit rewrite modules unless remediation is requested; review evidence must precede edits.

| `long-context-corpus` | `stateblock`, `sequential-memory-state-engine`, `stable-long-context`, `activation-budget-funnel`, `drift-suppression` | None | PASS |

**Boundary:** Use when the authorized corpus cannot be handled safely as one undifferentiated context and indexed state must preserve sequence and provenance.

**Required rationale:** `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because segmented corpus work needs durable provenance, bounded active context, and drift-resistant state. `sequential-memory-state-engine` — Preserve sequence, provenance, relevance, and current truth across long-running work. It is required here because segmented corpus work needs durable provenance, bounded active context, and drift-resistant state. `stable-long-context` — Extend usable context duration without treating the entire transcript as equally current or important. It is required here because segmented corpus work needs durable provenance, bounded active context, and drift-resistant state. `activation-budget-funnel` — Protect limited active context by progressively disclosing sources and transferring verified evidence into compact indexed state before higher-level decisions. It is required here because segmented corpus work needs durable provenance, bounded active context, and drift-resistant state. `drift-suppression` — Keep execution aligned after distracting context, repeated transformation, or model error. It is required here because segmented corpus work needs durable provenance, bounded active context, and drift-resistant state.

**Important exclusion:** Omit full-corpus reloads and alternative-branch generation unless explicitly triggered.

| `authoring` | `task-set-lock-in`, `safe-rewrite`, `placeholder-suppression` | None | PASS |

**Boundary:** Use when drafting or rewriting a controlled deliverable with an explicit message, audience, style, source, or placeholder contract.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the controlled deliverable must retain its locked message and cannot ship unresolved template residue. `safe-rewrite` — Make paraphrase, polish, tone, or formatting safe by treating content atoms as invariants rather than suggestions. It is required here because the controlled deliverable must retain its locked message and cannot ship unresolved template residue. `placeholder-suppression` — Prevent scaffolding artifacts from escaping as if they were complete content. It is required here because the controlled deliverable must retain its locked message and cannot ship unresolved template residue.

**Important exclusion:** Omit citation fidelity for source-free drafting and omit structural surgery for tonal edits.

| `creative-ideation` | `task-set-lock-in`, `controlled-drift-corridors` | `multiverse-reasoning` | PASS |

**Boundary:** Use when the user needs multiple materially distinct concepts followed by bounded convergence, while factual claims remain outside the creative corridor.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because creative search still needs a locked brief and an explicit boundary separating invention from factual assertion. `controlled-drift-corridors` — Enable adaptation, compression, or creativity without surrendering semantic control. It is required here because creative search still needs a locked brief and an explicit boundary separating invention from factual assertion.

**Important exclusion:** Omit high-stakes evidence gates unless factual claims enter the deliverable.

| `education-explanation` | `pedagogical-alignment`, `task-set-lock-in` | None | PASS |

**Boundary:** Use when an explanation must be adapted to a named learner, objective, and prerequisite level while retaining domain accuracy.

**Required rationale:** `pedagogical-alignment` — Make correct content learnable and usable for a specified audience without diluting claims or inventing simplifications. It is required here because the learning objective and audience model must remain fixed while the explanation is adapted. `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the learning objective and audience model must remain fixed while the explanation is adapted.

**Important exclusion:** Omit deep validation stacks for low-risk exposition; retain accuracy checks that the domain requires.

| `decision-support` | `task-set-lock-in`, `decision-first-scaffold`, `grounding-no-invention` | None | PASS |

**Boundary:** Use when a user must compare consequential options against explicit criteria and evidence without turning generated candidates into facts.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the decision question, option criteria, and factual evidence boundary must remain explicit. `decision-first-scaffold` — Keep analysis shaped around a decision, options, and decision criteria rather than accumulating directionless detail. It is required here because the decision question, option criteria, and factual evidence boundary must remain explicit. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because the decision question, option criteria, and factual evidence boundary must remain explicit.

**Important exclusion:** Omit Multiverse when options are already fixed; never turn candidate generation into fake evidence.

| `architecture-skill-building` | `architect-orchestrator`, `scoped-loader`, `stateblock`, `parallel-qms` | `architect-orchestrator`, `power-mode`, `hybrid-mode`, `multiverse-reasoning`, `behavior-gene-builder`, `domain-core-builder`, `meta-supervisor`, `adapter-first-experimentation`, `surgery-edit`, `future-proof-mode-selector` | PASS |

**Boundary:** Use when designing or restructuring a portable multi-component Skill whose interfaces, authority order, and host assumptions must be explicit.

**Required rationale:** `architect-orchestrator` — Plan and coordinate modular system design from goal discovery through critique, localized repair, synthesis, and continuation state. It is required here because multi-component Skill design needs one coordinator, selective loading, explicit state, and a validation topology. `scoped-loader` — Keep modular OS or Skill execution relevant, ordered, and within context limits instead of loading the full library at session start. It is required here because multi-component Skill design needs one coordinator, selective loading, explicit state, and a validation topology. `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because multi-component Skill design needs one coordinator, selective loading, explicit state, and a validation topology. `parallel-qms` — Match validation topology to failure risk instead of treating QMS as one generic critic or a majority vote. It is required here because multi-component Skill design needs one coordinator, selective loading, explicit state, and a validation topology.

**Important exclusion:** Omit suite-level supervision for a single small Skill and omit surgery for additive changes.

| `multi-agent-orchestration` | `architect-orchestrator`, `scoped-loader`, `state-routing-bus`, `stateblock`, `state-snapshot`, `domain-mode-isolation` | `architect-orchestrator`, `state-routing-bus` | PASS |

**Boundary:** Use only when the host can run or coordinate distinct workers and the task needs explicit routing, state exchange, and result collection.

**Required rationale:** `architect-orchestrator` — Plan and coordinate modular system design from goal discovery through critique, localized repair, synthesis, and continuation state. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs. `scoped-loader` — Keep modular OS or Skill execution relevant, ordered, and within context limits instead of loading the full library at session start. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs. `state-routing-bus` — Pass explicit task state, decisions, evidence pointers, and module outputs through real host-supported handoffs. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs. `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs. `state-snapshot` — Create a stable checkpoint that can be resumed or audited after interruption. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs. `domain-mode-isolation` — Prevent cross-domain contamination while permitting explicit, reviewed transfers of shared facts. It is required here because distinct workers need a coordinator, scoped loading, routed shared state, domain separation, and recoverable handoffs.

**Important exclusion:** Omit distributed claims when the host cannot provide isolated workers or result collection.

| `deterministic-intake-routing` | `task-set-lock-in`, `grounding-no-invention`, `scoped-loader`, `domain-mode-isolation`, `stateblock`, `authenticity-anti-evasion` | None | PASS |

**Boundary:** Use when supplied fields and explicit predicates should deterministically select a route, with clarification or fail-closed handling for missing inputs.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data. `scoped-loader` — Keep modular OS or Skill execution relevant, ordered, and within context limits instead of loading the full library at session start. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data. `domain-mode-isolation` — Prevent cross-domain contamination while permitting explicit, reviewed transfers of shared facts. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data. `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data. `authenticity-anti-evasion` — Keep process-status and completion claims auditable, especially when the host lacks a requested source, tool, persistent state, or execution capability. It is required here because the route must remain grounded in supplied fields, isolated by domain, and honestly report missing capability or data.

**Important exclusion:** Omit model-judged routing when deterministic predicates fully decide the route.

| `long-context-source-fidelity` | `working-memory-lock-in`, `sequential-memory-state-engine`, `stateblock`, `stable-long-context`, `zero-drift-zones`, `drift-suppression`, `fail-closed-abstention` | `fail-closed-abstention`, `truth-redundancy` | PASS |

**Boundary:** Use when a long or segmented source must be transformed without losing sequence, quotations, identifiers, or provenance across context boundaries.

**Required rationale:** `working-memory-lock-in` — Prevent critical goals, constraints, identifiers, or safety conditions from being displaced by incoming context. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `sequential-memory-state-engine` — Preserve sequence, provenance, relevance, and current truth across long-running work. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `stateblock` — Give tools, agents, validators, and handoffs a shared source of current task truth. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `stable-long-context` — Extend usable context duration without treating the entire transcript as equally current or important. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `zero-drift-zones` — Protect facts, identifiers, quotations, obligations, safety limits, and other high-consequence content from transformation drift. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `drift-suppression` — Keep execution aligned after distracting context, repeated transformation, or model error. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps. `fail-closed-abstention` — Ensure that missing essential support produces an explicit bounded result rather than fabricated closure. It is required here because source continuity requires locked working facts, sequential provenance, protected exact atoms, drift control, and abstention on gaps.

**Important exclusion:** Omit persistence when one context suffices and omit citation certification for inaccessible sources.

| `perception-reasoning` | `task-set-lock-in`, `grounding-no-invention`, `anti-tunnel-vision`, `bounded-exit`, `micro-scaffolding` | `multiverse-reasoning` | PASS |

**Boundary:** Use for bounded integer-grid or symbolic transformation tasks with at least one training pair and a test input; disclose reduced verification when only one pair is available.

**Required rationale:** `task-set-lock-in` — Prevent scope substitution and goal drift during execution. It is required here because the solver must lock the task, derive rules only from examples, falsify premature hypotheses, and terminate boundedly. `grounding-no-invention` — Prevent fabricated facts, citations, measurements, policies, records, and gap-filling in source-grounded work. It is required here because the solver must lock the task, derive rules only from examples, falsify premature hypotheses, and terminate boundedly. `anti-tunnel-vision` — Preserve enough search breadth to expose premature fixation, then collapse quickly when evidence discriminates. It is required here because the solver must lock the task, derive rules only from examples, falsify premature hypotheses, and terminate boundedly. `bounded-exit` — Turn iterative improvement into a terminating control loop with explicit quality, budget, and diminishing-return gates. It is required here because the solver must lock the task, derive rules only from examples, falsify premature hypotheses, and terminate boundedly. `micro-scaffolding` — Protect a difficult local operation without loading the full OS, duplicating the parent StateBlock, or leaving permanent context residue. It is required here because the solver must lock the task, derive rules only from examples, falsify premature hypotheses, and terminate boundedly.

**Important exclusion:** Omit coherence heartbeat and meta supervision for bounded single-puzzle work; add candidate branching only when hypotheses genuinely compete.

